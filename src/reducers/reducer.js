// @flow

type actionType = {
  +type: string,
};

type reducerType = {

};

const products = (
  state: reducerType = {},
  action: actionType,
) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default products;
